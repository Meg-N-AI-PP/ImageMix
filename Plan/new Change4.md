# Changes:
1. New screen call Video Scene, for this screen, user can create a video prompt for 2 images (start/end frames) and based on these option:
 - Camera Position Change: there will be a selection for camera position, it will have lot of options too choose, user can choose from it, can add new camera style, and there is an suggestion feature that if user click on it, it will based on the start image and end image to pick the best camera position based on the available one. User can select none so the camera will be like the start image.
 - Sound: enable it and t he prompt will have sound detail that match these image and what user want
 - Main action: user describe very simple about the action in the scene
 - Environment change: from low/medium/high for selection, the more level there more current environment and things in the image scene change
So after user done with these options, they click generate scenes, and it will return to user three video prompts which is short prompt, medium prompt and large prompt, and user can copy any prompt they want to use.

2. In the scene screen when they done generated image for the selected image (image 2, not the consistent image), an option or button will appear call "Generate Video Prompt?", and when user click it will navigate to the Video Scene and filled the start image (selected image 2) and the end image (the generated image), and user can continue on that.

3. There is an issue with the library screen, like user can't see the show, view, fusion option clearly for all the images, some of it has and mostly it not show, or it being cover by the image, please check and fix
