# Changes
1. I want another screen for Editing an image or can add it in Fusion screen and add mode Fusion or Editing, for Editing logic is simple, user add the image they want to edit and then write a prompt on how they want to edit, also can add another images to edit.
2. and for every screen, after generating image, I can right mouse click the image and copy, then I can paste it to any chat I want.
3. I want another screen for last image after a scene (video), the logic is like this:
    - User select an image, and write a description simple on what from the image will happened in [number] seconds (user can config the seconds)
    - There is an option to enchance prompt with their description and the image and in selected number seconds, if they use it, it will enchance the description they writed.
    - Then they click generate (the button name is Next Image), and it should generated the image after the description happened for that image after selected number seconds, so if its 8 seconds then the generated image should show what change in the second 8.