# Changes:
1. I need changes in the scene screen to have consitence character, I think like this:
    - now its have check box for enable consistent character, when enable, an image select will appear (as image 1), and a text input for user to input a desciption about consistent character, something like "Based on this leviathan as consistent monster through all image".
    - then user select another image (image 2) which include or not include the consistent character like image 1, and still the same, they make the detail about the scene in seconds that can include or not include the consitent character, the enchanced prompt if its use need to detail about the consistent character or must said use consistent character from image 1.
    - then they can generate the next image with the consitent character

    - If user don't select that checkbox to enable consistent character, then everything work like before.